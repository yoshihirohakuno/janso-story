# 雀荘物語 開発進捗状況

本格リーチ麻雀 × 雀荘経営シミュレーション × ドット絵RPGゲーム『雀荘物語（仮）』のフェーズ1（麻雀エンジンおよび基本UI）の開発進捗状況です。

---

## 📅 現在のステータス: **フェーズ1（麻雀エンジン開発）完了**

日本式リーチ麻雀の基本ルールに基づいた正確なゲームエンジン、ボット（AI）対戦ロジック、および高品質なリアル調ウェブUIの実装が完了し、本番向けビルドおよび自動テストがすべてパスしている状態です。

---

## 🛠️ 完了した実装項目

### 1. 麻雀エンジン（`src/engine/`）
* **データモデル設計 (`types.ts`)**: 牌（赤ドラ含む）、手牌、副露（チー・ポン・カン）、捨牌、局状態、点数などを表現する堅牢なTypeScript型定義。
* **壁牌・山制御 (`wall.ts`)**: 136枚の牌のシャッフル（Fisher-Yates）、配牌、ドラ表示牌・裏ドラ表示牌、嶺上牌の制御。
* **向聴数（シャンテン数）計算 (`shanten.ts`)**: 一般手、七対子、国士無双の3パターンに対応した高速バックトラッキング計算。
* **役判定ロジック (`yaku.ts`)**: 平和、断么九、一盃口、二盃口、混一色、清一色、大三元、四暗刻などの全標準役の正確な自動判定と、高得点パターンの選択（高得点法）。
* **得点計算ロジック (`scoring.ts`)**: 符計算（基本符、待ち、構成等による加符）、親・子別の点数授受、本場/供託の移動、飛び終了、および**切り上げ満貫**の切り替え対応。
* **ゲームループ・ステートマシン (`game.ts`)**: ツモ、打牌、鳴き優先度判定（ロン > ポン/カン > チー）、フリテン判定（同巡フリテン、一時フリテン、リーチフリテン）、および流局判定（九種九牌、四風連打、四家立直、四開槓、四人リーチ）。
* **ゲーム管理 Zustand ストア (`store.ts`)**: 全プレイヤーおよびオート対戦ボットの行動制御を一元管理するグローバルストア。

### 2. インタラクティブUI（`src/ui/`）
* **ゲーム集中型 UI とフローティングサイドバーの導入 (NEW)**:
  * **スライドイン・ドロワー式サイドバー**: 右側のスコアボード、ログ、デバッグ設定用サイドバーをフローティング（絶対配置）に変更。デフォルト非表示（collapsed）とし、画面中央にゲーム盤が大きく配置されるよう改善。
  * **スマートなトグルボタン**: サイドバーの開閉トグルボタン `📋` はサイドバーの開閉に連動してスライド移動し、開いた状態でも操作可能。
  * **高級感のある半透明グラスモルフィック**: サイドバーには `blur(20px)` を効かせた半透明デザインを採用し、背後のテーブルが美しく透過する仕様に。
  * **麻雀卓の表示エリア最大化**: テーブルのサイズを `min(92vh, 92vw)`、最大サイズを `860px`（従来の `780px` から拡大）とし、アスペクト比 1:1 を保ちながら画面全体でゲームに没頭できるよう改善。
  * **プレイヤーバッジの配置最適化**: 各プレイヤーのスコアバッジが誰のものか直感的に一目で理解できるよう、それぞれの辺（手牌の内側）に揃えて配置を最適化しました。
    * **自分 (下)**: 手牌の左上の位置 (`left: 25px; bottom: 75px;`)
    * **下家 (右)**: 右側の中央より少し下、右の手牌の内側に配置 (`right: 75px; bottom: 160px;`)
    * **対面 (上)**: 上側中央、上の手牌の真下の内側に配置 (`left: 50%; top: 75px;` 中央揃え)
    * **上家 (左)**: 左側の中央より少し上、左の手牌の内側に配置 (`left: 75px; top: 160px;`)
    * これにより、手牌や河（捨て牌）との重なりを完全に防ぎつつ、各辺の座席位置とバッジ位置が一致し、誰の持ち点なのかが瞬時にわかる直感的な設計に改善しました。
    * **座席風表示の日本語化 (NEW)**: 各プレイヤーのスコアバッジ、サイドバー、リザルト画面に表示される風の表記を、英文字（E/S/W/N）から日本人に直感的な漢字の「東・南・西・北」に統一しました。
  * **左右手牌の配置中央揃え修正**: 左右のプレイヤーの手牌が画面外に切れて見えなくなっていた問題と、捨て牌（河）との重なりを修正。
    * 手牌の回転基準点を中央 (`transform-origin: center center`) に変更し、動的な手牌の枚数（幅）の半分を打ち消しつつ、タイル高さの半分（16.5px）を考慮した CSS `calc` 式（`translateX(calc(50% - 16.5px))` および `translateX(calc(-50% + 16.5px))`）を導入しました。
    * これにより、手牌の残り枚数や副露数に関わらず、常に卓の左右の両端（15px マージン）に完全に密着し、かつ上下（縦軸）も完全に美しく中央揃えになる対称レイアウトへと改善しました。
    * **対戦相手手牌の3D立体起立化 (NEW)**: 対戦相手の伏せ牌（裏向きの手牌）に、卓上に直立して立っているように見える立体的な厚み（アイボリー/クリーム色の境界層 `border-bottom: 4px solid #dfd3b5`）を施し、リアルな3D卓の雰囲気を再現しました。
* **3D調麻雀卓レイアウト (`MahjongTable.tsx`)**: 高級感ある緑のラシャ布をイメージしたグラデーション背景と、各座席に配置された手牌・捨牌の河（それぞれ90°/180°/270°回転対応）。
* **非重複捨牌ポン（河） (`DiscardPond.tsx`)**: リーチ宣言牌を横向き、鳴かれた牌をグレーアウトして6列×3行に収まるように配置。他プレイヤーと被らないよう衝突回避を徹底。
* **カラーSVG牌画の統合とデザイン修正**:
  * 従来のモノクロ Unicode キャラクターを廃止し、`riichi-mahjong-tiles` ベクターアセット（カラーSVG）を統合。オフラインでも動作するローカル配信を実現。
  * **字牌の文字色修正**: 風牌（東・南・西・北）の彫り文字が青色（`#142896` / `#19328c`）で表現されていたデザインを、日本国内で一般的な美しい**「黒色（`#000000`）」**へと修正しました。
  * **ドラ表示牌の視認性向上修正 (NEW)**: 中央ボードの下部に表示されるドラ牌が見づらかった問題を改善。ドラ牌の表示サイズを `18px x 25px` から `22px x 30px` にアップスケールし、さらに表向きのアクティブなドラ表示牌に美しいゴールドの細枠と光彩（ゴールドグロー）のエフェクトを追加して、卓上での存在感を大幅に高めました。「ドラ表示牌」のタイトル文字もゴールドイエローに変更し、太字にしてアピールを強化しています。
* **トビ終了時の理由明記とログ追加 (NEW)**:
  * 東1局などの早期に大物手があがり、誰かの持ち点が0点未満（マイナス）になって対局が終了（トビ終了）した際、理由が分からず突然ゲームが終了したように見える問題を改善しました。
  * 局終了時のモーダルタイトルに「対局終了 (〇〇 のトビ終了)」とトビになったプレイヤーの名前を明確に表示。
  * 対局ログにも「⚠️ 〇〇 が 0点未満（トビ）となったため、対局終了となりました。最終結果を表示します。」と状況を出力し、ゲームの終了理由を分かりやすく改善しました。
* **スクロール不要な画面設計**:
  * **初期ロビー設定画面（トップページ）の最適化**: 画面全体のパディングやマージン、ヘッダーロゴサイズ、プレイヤー枠の高さ、対局ルールのグリッド余白を徹底的に圧縮。これにより、スクロール不要で最下部の「対局開始 (配牌へ)」ボタンが常に画面内に表示されるように改善。
  * **局終了時リザルトモーダルの最適化**: モーダル各部のパディング・余白を詰め、和了手牌や副露牌（チー・ポン・カン）の表示サイズを手牌サイズから副露用のコンパクトサイズ（`24px x 33px`）に縮小。画面内にすべての情報を収め、スクロールなしで「次の局へ進む」ボタンをクリックできるように改善。
* **和了（ロン・ツモ）時のテーブル上での演出強化 (NEW)**:
  * **和了手のリアルタイム公開**: ロンまたはツモの和了宣言時、結果モーダルが開くまでの1.5秒間の猶予時間内に、**和了プレイヤーの手牌が卓上で即座に表向き（公開状態）**になるように改善しました。
  * **和了牌（アガリ牌）の分離とネオン発光**:
    * **ロンあがり**: ロンされた放銃牌（他人の捨て牌）が河の中で赤く脈打って発光（ロンターゲットグロー）し、同時にその牌が和了者の手牌の右側に「ツモ牌」のように分離して配置され、赤色ネオン（`#ff3366`）でパルス発光します。
    * **ツモあがり**: 和了者のツモ牌（右側に離れて置かれた牌）が紫色ネオン（`#cc33ff`）でパルス発光します。
  * **発声ボタンの即座消去**: ロン/ツモの発声ボタンをクリックした直後、和了演出（1.5秒）の間やそれ以降にボタンが画面に残らないよう、発声判定と同時にアクティブコール一覧をクリアし、ボタン群を即座に非表示化する仕様に改善しました。
* **プレミアム演出効果の追加**:
  * **立致（リーチ）および副露（チー、ポン、カン、ロン、ツモ）時のネオン演出**: アクション発生時、画面中央にプレイヤー名とアクション名がダイナミックなネオンカラー（立直＝金、ロン＝赤、ツモ＝紫、ポン・チー・カン＝水色）で点滅しながらカットインするスライドバナーを追加。
  * **得点授受のアニメーション（イージング）**: リザルトモーダルの得点移動リストにて、スコア変動がスクリプト制御でイージングしながらリアルタイムでカウントアップ/カウントダウンする演出を導入。
  * **得点変動値のパルス発光**: 増加（緑）や減少（赤）の差分スコアが脈打つように発光・ポップアップするアニメーションを適用。
* **リアルな打牌ペースの導入**:
  * 打牌後に **1000ms（1秒）** の猶予を設け、捨てられた牌の確認をしやすく改善。
  * ボットツモ時に **800ms** の思考時間（ディレイ）を導入し、リアルな対局感を再現。
  * 鳴き「パス」時は **200ms** で高速進行させ、快適なプレイテンポを両立。
* **ガイド表示の非表示化**: 牌の左上に小さく描かれていたデバッグ/ユニバーサル用の文字ラベル（`2m`, `5pr`等）を削除し、純粋な美しい牌の見た目に修正。

---

## 🧪 テスト・動作検証

* **自動テスト**: `src/engine/tests/engine.test.ts` にて10項目のユニットテスト（シャンテン計算、役判定、符・点数計算）を実装し、すべてパスしています。
* **本番用ビルド**: Viteコンパイラによる `npm run build` が正常に機能することを確認済みです。

---

## 🔮 フェーズ2（経営シミュレーション＆RPG）に向けたタスク

1. **セーブ・ロード機能**: Zustandストアと `localStorage` を連携させた対局中データおよびオーナー成績の保存システム。
2. **雀荘経営要素の実装**:
   * 店舗（雀荘）の資本金・売上管理。
   * 新しい麻雀卓（全自動卓など）やドリンク、フードメニューの購入・アップグレード。
   * 雀荘に訪れるNPC客の性格（打ち筋、マナー、支払う場代など）の定義。
3. **ドット絵RPGパートの構築**:
   * マップ（店舗内、街など）の移動・探索。
   * ライバル雀士との会話・遭遇、賭け麻雀・大会へのエントリーシステム。
4. **ゲームの演出強化**:
   * リーチ、ポン、チー、ロン、ツモ時の視覚的カットインやエフェクトの追加。
   * BGM（対局中、和了時など）と効果音（牌の打鍵音、発声ボイスなど）の組み込み。

---

## 🧭 フェーズ2 RPGプロトタイプ進捗（2026-06-13 追加）

### 実装済み

* **RPGパートの初期画面化**: アプリ起動時に雀荘内マップを表示し、既存の麻雀ロビーは「対局ロビー」ボタンから開ける構成へ変更。
* **雀荘内マップ**: 添付の白龍亭イメージを基に、カウンター、麻雀卓、ソファ、自販機、掲示板、入口を持つ2D見下ろし型マップを実装。
* **主人公移動**: キーボード（矢印/WASD）と画面Dパッドで上下左右に移動可能。壁、卓、カウンター、NPCとの衝突判定を実装。
* **NPC会話**: 黒川、健太、タケ爺、常連客、氷室レイナの会話データを段階別に持てる構造を追加。会話ウィンドウで名前とセリフを表示。
* **ストーリー進行管理**: `tutorial_before`、`tutorial_match_started`、`tutorial_after`、`shop_entrusted`、`regular_match_unlocked`、`rival_appeared` などの進行ステージを定義。
* **麻雀対局連携**: 店主の前で「卓につく」を選ぶと、既存の麻雀対局画面へ遷移。RPGイベント対局中のみ、プロトタイプ用の勝利/敗北結果反映ボタンを表示。
* **対局結果の仮反映**: 勝利時に所持金、評判、来客数、常連客数、対局成績が更新され、「白龍亭を託された日」へ進行。
* **雀荘経営数値**: 所持金、評判、店舗レベル、来客数、常連客数、対局成績を管理。
* **店舗強化**: 卓追加、看板更新、ドリンクメニュー追加、週末大会開催の購入処理を追加。週末大会でライバル登場フラグへ接続。
* **簡易セーブ/ロード**: RPG進行、経営数値、現在位置、解放済みキャラクター、対局成績、店舗強化状態を `localStorage` に保存。
* **雀荘背景の忠実化 (NEW)**: 添付の「雀荘風景.png」を `public/rpg/janso-parlor.png` として取り込み、RPGマップをCSSタイル描画から背景画オーバーレイ方式へ変更。背景画の上に主人公スプライト、NPC会話ポイント、移動/会話操作UIを重ねることで、白龍亭のカウンター、麻雀卓、窓、ソファ、自販機、入口まわりの雰囲気をそのまま使える構成に改善。
* **キャラクター仕様書の構造化 (NEW)**: 美咲、健太、黒川、氷室レイナ、タケ爺について、見た目、16x24スプライト、256x256ポートレート、性格、麻雀傾向、会話、ストーリー上の役割、店舗システム連携、そして共通の `art_style` を `src/rpg/characterSpecs.ts` に機械可読なTypeScriptデータとして定義。
* **白龍亭マップ仕様書の構造化 (NEW)**: `src/rpg/mapSpecs.ts` に `hakuryutei_main` を定義。30x20 グリッド、4卓、受付カウンター、自販機、雑誌ラック、ソファ、下駄箱、卓エリア/受付/待合/入口などのナビゲーションゾーン、`first_visit` / `owner_talk` / `tournament` のイベント定義、環境アートバイブル、衝突判定エリア、スポーン地点をまとめて管理する構成へ移行。
* **仕様駆動の移動/会話判定 (NEW)**: RPG側のマップサイズ、初期スポーン位置、NPC配置、衝突判定、現在地表示、イベント表示を `mapSpecs` 参照に変更。特に黒川はカウンター奥に配置したまま、受付前から会話できる専用インタラクションポイントを追加。
* **キャラスプライト制作仕様の追加 (NEW)**: 各主要キャラクターに、16x24フレーム基準・5列×20行・80x480px の共通スプライトアトラス仕様、アニメーションブロック順、フレーム数、書き出しファイル命名規則を追加。勝利モーションだけはキャラごとの実フレーム数差分も持てる構成に整理。
* **ポートレート表情差分の仕様化 (NEW)**: 主要5キャラすべてに、256x256肖像向けの `expression_set` を追加。眉・目・口・首角度・頬色・利用シーンまで定義し、単なる表情名の列挙から制作指示書として使える粒度へ更新。
* **キャラクターのマップ利用仕様追加 (NEW)**: 各キャラに `map_usage` を追加し、白龍亭内での既定ゾーン、導線上の役割、進行段階ごとの出現目的を明文化。主人公・店主・案内役・常連・ライバルの見せ方をデータで追える状態にした。
* **白龍亭のステージ別シーンレイアウト化 (NEW)**: `mapSpecs` に `stage_layouts` を追加し、進行段階ごとのNPC配置、卓の稼働状態、シーンのムード、発火イベントを定義。 `data.ts` 側のNPC表示はこのレイアウトから生成する構成へ移行し、ハードコードされた配置定義を削減。
* **RPG画面への仕様参照UI追加 (NEW)**: 右パネルに「シーン配置」「主人公仕様/注目キャラ」を追加。現在ステージの配置NPC、イベントタグ、卓稼働数、対象キャラの打ち筋、スプライトシート寸法、表情差分、テーマカラーを即確認できるようにした。
* **NPC実体化と巡回移動の追加 (NEW)**: 会話マーカー主体の表示をやめ、NPCを実スプライトとして描画する構成へ変更。健太、タケ爺、常連客、レイナは進行段階ごとの短い巡回ルートを持ち、RPG画面上で定期的に歩行・停止を繰り返す。会話対象の判定もその現在位置に追従するよう調整した。
* **主人公スプライトの先行画像化 (NEW)**: 美咲だけ先に4方向の実ドット絵スプライトを試作し、`public/rpg/misaki-sheet-source-v1.png` を読み込んで主人公表示のみ画像ベースへ切り替えた。緑背景はブラウザ側で透過処理する方式を採用。
* **主人公の表示倍率検証 (NEW)**: 背景に描き込まれた店内客の頭身と近づけるため、主人公スプライトの表示枠と画像占有率を複数回調整。現時点では「主人公だけ先に実スプライト表示し、背景人物との縮尺感を探るプロトタイプ段階」と位置付けている。
* **主人公スプライトのサイズ確定作業継続 (NEW)**: 背景の常連客と比較してなお小さく見えるため、主人公だけ別倍率で拡大する調整を継続。NPC側の見え方は崩さず、美咲だけを先行して基準サイズへ合わせる方向で詰めている。
* **美咲16枚スプライトの実装 (2026-06-15 NEW)**: ユーザー作成の16枚PNGを `public/rpg/misaki-walk-frames/` に取り込み、`1-4=上`、`5-8=右`、`9-12=下`、`13-16=左` の4方向×4コマとして主人公表示に接続。旧来の横一列スプライトシートとブラウザ側の緑背景透過処理に依存せず、透過PNGを直接切り替える方式へ移行した。
* **主人公サイズの再調整 (2026-06-15 NEW)**: 背景に描き込まれた人物より大きく見えすぎていたため、主人公の表示枠を縮小。デスクトップ相当表示では約 `108x136px` の表示枠に調整し、背景人物との縮尺差を抑えた。
* **ドラクエ風の滑らか移動 (2026-06-15 NEW)**: プレイヤー移動を「グリッド座標の即時ジャンプ」から、旧座標から次座標へ `left/top` を `230ms linear` で補間する方式へ変更。内部の当たり判定とストーリー座標は従来通りグリッド管理しつつ、画面表示だけ1マス分を滑らかに移動する構成にした。
* **押しっぱなし移動対応 (2026-06-15 NEW)**: 矢印キー/WASDの `keydown` で移動方向を保持し、`keyup` まで連続して1マス移動を繰り返す制御を追加。画面Dパッドもポインター長押しで連続移動できるようにし、フォーカス喪失や会話開始時には保持方向を解除する安全処理も追加した。
* **ビルド環境および mapSpecs.ts の復旧 (2026-06-17 NEW)**: 前回のセッション中断時に生じた `src/rpg/mapSpecs.ts` の構文エラー・ファイル破損を解消し、Git HEAD状態へ修復。プロジェクト全体のビルド（`npm run build`）およびユニットテスト（`npm run test`）がすべて正常にパスすることを確認。
* **雀荘背景に合わせた導線・当たり判定の再調整 (2026-06-17 NEW)**: `sangenrou-interior.png` の実表示に合わせて、左本棚、ソファ、カウンター、4卓と椅子、左右下壁の衝突矩形を再定義。入口から中央通路を通って受付前へ移動でき、卓・椅子・カウンター上には入れない導線へ修正した。
* **健太の初期配置と巡回ルート修正 (2026-06-17 NEW)**: 健太が卓上や中央導線を塞ぐ位置に出ないよう、初回来店時は左側通路に配置し、各ステージの巡回点も卓・椅子の衝突範囲外へ調整。受付へ向かう主導線を塞がないようにした。
* **店主黒川のカウンター越し表示調整 (2026-06-17 NEW)**: 黒川のスプライト表示倍率を店主用に拡大しつつ、下半身をカウンター裏に隠すクリップ表示へ変更。カウンター上に全身で立って見える違和感を抑えた。

### 主な追加ファイル

* `src/rpg/types.ts`
* `src/rpg/data.ts`
* `src/rpg/store.ts`
* `src/rpg/components/RpgScene.tsx`
* `src/rpg/index.css`
* `src/rpg/characterSpecs.ts`
* `src/rpg/mapSpecs.ts`
* `public/rpg/janso-parlor.png`
* `public/rpg/misaki-sheet-source-v1.png`
* `public/rpg/misaki-walk-frames/edited-photo_1.png` - `edited-photo_16.png`
* `docs/design/06_tutorial_spec.md`

### 次に進める候補

1. **町・商店街・大会会場など複数マップへの切り替え**: マップ遷移と表示制御の実装。
2. **会話イベントの選択肢、イベントカットシーンの専用演出**: 選択肢分岐や店主失踪シーンの構築。
3. **麻雀対局の実終了結果をRPGに返す処理の本実装**: 対局結果（順位・点数）のセーブデータ・経営数値へのフィードバック。
4. **常連客対局、ライバル対局、大会エントリーのイベントテーブル化**: 対局相手編成などのシステム化。
5. **健太・黒川・タケ爺・レイナも美咲と同じ16枚PNG方式で画像化**: NPCの歩行アニメーション表示の刷新。
6. **操作性と当たり判定の微調整**: 主人公の移動速度や判定、通路幅などのプレイ感向上。
7. **チュートリアルシステム（初心者モード）の実装**: 初回起動時の選択、ヒント吹き出し、和了時の役解説、三元楼の教科書などの実装。

